import type ThemesConfigTypes from "@/types/themes_config";

const config: ThemesConfigTypes = {
    illustrations: {
        set: "sketchy-1",
    },
    icons: {
        style: "duotone",
    },
};

export default config;
